import { SelectItemPipePipe } from './select-item-pipe.pipe';

describe('SelectItemPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectItemPipePipe();
    expect(pipe).toBeTruthy();
  });
});
